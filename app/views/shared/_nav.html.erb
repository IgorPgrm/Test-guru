<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to 'TestGuru', root_path, class:'navbar-brand'%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <%= link_to t('.tests'), tests_path, class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <%= link_to t('.about'), "/about", class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <%= link_to t('.contacts'), "/about/author", class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <%= link_to t('.feedback'), "/feedback", class: 'nav-link' %>
      </li>
      <% if user_signed_in? %>

        <% end %>
      <li class="nav-item">
        <% if current_user&.is_admin? %>
        <li class="nav-item">
          <%= link_to 'Gists', admin_gists_path, class: 'nav-link' %>
        </li>
        <%= link_to 'AdminPanel', admin_tests_path, role: "button", class: 'nav-link btn btn-success' %>
        <% end %>
      </li>
    </ul>
  </div>
  <span class="navbar-text">
  <% if user_signed_in? %>
      <p class="nav user"><b class="user-email"><%= current_user.email %></b> Guru | <%= link_to t('shared.header.logout'), destroy_user_session_path, method: :delete %></p>
    <% else %>
    <%= link_to t('shared.header.login'), new_session_path(resource_name) %>
      <%= link_to t('shared.header.signup'), new_registration_path(resource_name) %>
  <% end %>
</span>
</nav>


<nav class="navbar navbar-light bg-light">
  <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>
  <span class="navbar-text">
  <% if user_signed_in? %>
      <p class="nav user"><b class="user-email"><%= current_user.email %></b> Guru | <%= link_to t('shared.header.logout'), destroy_user_session_path, method: :delete %></p>
    <% else %>
    <%= link_to t('shared.header.login'), new_session_path(resource_name) %>
      <%= link_to t('shared.header.signup'), new_registration_path(resource_name) %>
  <% end %>
  </span>
</nav>
